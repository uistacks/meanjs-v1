(function () {
  'use strict';

  angular
    .module('inventories.routes')
    .config(routeConfig);

  routeConfig.$inject = ['$stateProvider'];

  function routeConfig($stateProvider) {
    // Intentionally removed front end routes
  }

}());
